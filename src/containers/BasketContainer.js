import MenuItem from "../components/Basket";
import { connect } from "react-redux";
import { } from "../actions";

function mapStateToProps(state) {

  return {
    
  };
}

const mapDispatchToProps = dispatch => {
  return {
    
  };
};

export default connect(
  mapStateToProps, mapDispatchToProps
)(Basket);
